{
  "name": "openai-proxy-worker",
  "version": "1.0.0",
  "description": "Proxy openai api requests with cloudflare worker",
  "main": "index.js",
  "scripts": {
    "check": "rome check src",
    "test": "echo \"Error: no test specified\" && exit 1",
    "deploy": "npm run check && wrangler publish src/index.ts",
    "dev": "wrangler dev src/index.ts --local"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "devDependencies": {
    "@cloudflare/workers-types": "^3.19.0",
    "itty-router": "^2.6.6",
    "rome": "^12.0.0",
    "typescript": "^4.9.5",
    "wrangler": "^2.15.1"
  }
}